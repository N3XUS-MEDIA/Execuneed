/**
 * P1-L-007 — implement createLeadAction against Prisma + scoreLead + consent + audit.
 * Support calls this from LeadForm. Do not add product recommendations here.
 */
export async function createLeadAction() {
  throw new Error('P1-L-007 not implemented')
}
